
'use client';

// This component is temporarily disabled because the external service
// (savefrom.net) blocks iframe embedding.
// The download functionality has been reverted to opening a new tab.
export function DownloaderModal() {
  return null;
}

    